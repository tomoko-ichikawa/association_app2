<%= form_with(model: @blog, local: true ,url: choose_new_or_edit ) do |f| %>

  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :content %>
    <%= f.text_field :content %>
  </p>
  <%= f.submit %>
<% end %>
