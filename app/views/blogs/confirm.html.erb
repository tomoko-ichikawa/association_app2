<h3>以下の内容で送信する</h3>
<%= form_with(model: @blog, url:blogs_path,local:true) do |f| %>
  <%= f.hidden_field :name %>
  <%= f.hidden_field :content %>

  <p>name:<%= @user.name %></p>
  <p>content:<%= @blog.content %></p>
  <%= f.submit %>
<% end %>
